{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "bun --watch index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/redis-config": "*",
    "axios": "^1.13.6",
    "resend": "^6.9.3"
  }
}
